# microtun-linux

The Linux Microtun daemon. It combines `microtun-std` with Linux TUN setup, host configuration, logging, and process lifecycle to run a Microtun tunnel as the `microtun` executable.
