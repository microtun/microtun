# microtun-ws

A small `no_std` WebSocket transport for Microtun control traffic. It implements the RFC 6455 client/server subset needed by the project with caller-owned buffers, `embedded-io-async` support, and optional Tokio adapters.
