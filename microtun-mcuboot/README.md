# microtun-mcuboot

Streaming validation for the MCUboot image envelope used by Microtun firmware updates. It verifies signed images, enforces anti-rollback policy, and supports read-back verification without implementing the bootloader itself.
