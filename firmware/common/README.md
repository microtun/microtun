# microtun-firmware-common

Board-independent support shared by the embedded firmware targets. It centralizes portable configuration and storage flows, CLI/Telnet behavior, network services, tunnel setup, reset handling, and firmware-update plumbing.
