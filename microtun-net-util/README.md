# microtun-net-util

Shared networking helpers for Microtun host tools and embedded firmware. It contains stable device naming and fallback addressing plus optional mDNS, DHCP, and ping support for `std` and Embassy environments.
