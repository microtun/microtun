# microtun-core

The sans-I/O secure tunnel engine at the heart of Microtun. It implements tunnel protocol state, sessions, routing, firewall policy, and dynamic peer resolution while leaving networking, timers, and runtime integration to embedding crates.
