# microtun-std

Tokio/`std` runtime integration for `microtun-core`. It provides the host tunnel runner, Peers API resolver, tunnel-device abstraction, and status reporting while leaving platform-specific interface setup to applications.
