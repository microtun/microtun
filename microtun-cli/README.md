# microtun-cli

A `no_std` async Telnet CLI framework for embedded Rust with clap-like derives. It provides bounded command parsing, editing, help/completion, Telnet handling, and static dispatch without requiring heap allocation by default.
