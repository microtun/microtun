# microtun-ymodem

A small async `no_std` YMODEM-1K/CRC implementation. It provides allocation-free sender and receiver primitives over standard `embedded-io-async` byte streams for both embedded and host transports.
