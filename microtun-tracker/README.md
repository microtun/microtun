# microtun-tracker

The Microtun Tracker service and tunnel endpoint. It maintains the peer registry, serves the in-tunnel Peers API, resolves dynamic peers, learns reachable endpoints, and reloads peer configuration while running.
