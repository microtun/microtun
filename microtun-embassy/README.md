# microtun-embassy

Embassy/`no_std` runtime integration for `microtun-core`. It drives the tunnel over `embassy-net`, exposes the tunnel as an Embassy network device, and integrates peer resolution and operational status for embedded targets.
